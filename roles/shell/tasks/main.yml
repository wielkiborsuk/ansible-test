---
- name: Install common tools
  package:
    state: latest
    name:
      - zsh
      - tmux
      - screen
      - cowsay
      - sc
      - weechat
      - figlet
      - tidy

- name: install antibody for zsh
  shell: curl -sL git.io/antibody | sh -s

- name: choose shell
  user:
    name: "{{ansible_user}}"
    shell: /usr/bin/zsh
